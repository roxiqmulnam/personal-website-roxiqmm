import React from "react";

const Footer = () => {
  return (
    <footer className="layout-component border-t-2 border-gray-500 text-right text-white">
      {new Date().getFullYear()} | Roxiq Mulna Muhammad
    </footer>
  );
};

export default Footer;
