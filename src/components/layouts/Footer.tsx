import React from "react";

const Footer = () => {
  return (
    <footer className="layout-component text-white">
        {new Date().getFullYear()} | Roxiq Mulna Muhammad
    </footer>
  );
};

export default Footer;
