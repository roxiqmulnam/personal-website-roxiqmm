import React from "react";

const Footer = () => {
  return (
    <footer className="layout-component text-white border-t-2 border-gray-500">
        {new Date().getFullYear()} | Roxiq Mulna Muhammad
    </footer>
  );
};

export default Footer;
